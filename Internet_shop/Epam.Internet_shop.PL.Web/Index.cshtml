@{
    Response.Redirect(@"/Pages/MainPage/Body/ShowcasePage.cshtml");

//string butesStr = "";

//if (IsPost)
//{

//    var file = Request.Files["p"].InputStream;

//    //System.Text.Encoding.UTF8.GetByte())
//    byte[] butes = new byte[file.Length];

//    file.Read(butes, 0, (int)file.Length);

//    butesStr = Convert.ToBase64String(butes);

//    Console.WriteLine();
//}
}


@*<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <title>Загрузка файла</title>
</head>
<body>
    <form enctype="multipart/form-data" method="post">
        <input type="file" id="file-test" name="p" accept="image/*">
        <input id="btn-test" type="submit" value="Отправить">
    </form>

    <img id="profileImage" src=""> data:image/jpg;base64,

</body>
</html>*@